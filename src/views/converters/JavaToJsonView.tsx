import { Heading } from '@chakra-ui/react';

const JavaToJsonView = () => {
  return (
    <Heading size='xl' marginBottom='8'>
      Java class to JSON converter
    </Heading>
  );
};

export default JavaToJsonView;
