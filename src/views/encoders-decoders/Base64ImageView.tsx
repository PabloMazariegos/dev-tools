import { Heading } from '@chakra-ui/react';

const base64ImageView = () => {
  return (
    <Heading size='xl' marginBottom='8'>
      Encode and decode Base64 to image
    </Heading>
  );
};

export default base64ImageView;